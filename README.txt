# Onx Install Notes
# Place Onx into your scripts folder
#
# Within Maya run the following code

#To include the 'done queue' swap out shelf command with onx = Onx.OnxFileManager(vis = True)

import Onx.install

